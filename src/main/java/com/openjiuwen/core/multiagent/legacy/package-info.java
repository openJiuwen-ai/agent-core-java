/**
 * Deprecated legacy multi-agent compatibility package.
 *
 * <p>Python `0.1.12` removed the legacy group-controller surface and moved the
 * platform toward newer team-oriented flows. These classes remain available only
 * so the copied `0.1.7` Java baseline can be migrated incrementally.
 */
@Deprecated(forRemoval = false)
package com.openjiuwen.core.multiagent.legacy;
