---
title: "[BUG] {{触发条件下出现的可观察故障}}"
---

## 问题摘要

{{用一句话说明在什么条件下发生了什么故障，以及受影响的行为}}

## 复现环境

- 目标分支或版本：{{分支、Tag 或 Commit}}
- JDK：{{例如 17}}
- 操作系统：{{操作系统与必要版本}}
- 相关模块：{{模块或包名}}

## 复现步骤

1. {{准备最小输入或前置状态}}
2. {{执行确定性的调用或操作}}
3. {{观察输出、异常或失败断言}}

## 实际结果

{{准确描述当前行为、异常类型、错误码或失败断言}}

## 预期结果

{{准确描述修复后的外部可观察行为}}

## 证据

```text
{{粘贴经过脱敏且最小化的日志、堆栈或测试失败；没有证据时填写“暂无”}}
```

## 代码定位

- 已确认存在的目标文件：{{仅填写真实存在的仓库相对文件；无法确认时填写“由 Agent 根据仓库证据检索确定”}}
- 相关类或符号：{{类、方法、字段或测试名称；未知时填写“待检索”}}
- 定位依据：{{说明为何怀疑该位置；未知时填写“根据复现行为检索调用链”}}

## 验收标准

- [ ] {{给出一个可观察、可判断的修复结果}}
- [ ] {{给出一个边界或回归场景}}
- [ ] 修改后通过 Java 编译 Gate；测试执行结果由人工或 CI 单独确认

## 变更边界

- 仅允许对 Java 主源码或测试源码进行最小修改。
- 不修改构建、CI、文档、示例、Skill、生成物或运行时文件。
- 不新增依赖，不降低安全校验，不访问外部服务。
