interface:
  display_name: "GitCode Issue Evolver Setup"
  short_description: "Set up and start the GitCode Issue Evolver service"
  default_prompt: "Use $gitcode-issue-evolver to validate local configuration, start the configured polling or webhook service, and report its non-secret status."
policy:
  allow_implicit_invocation: false
