# API文档

本章节按 Java 源码包结构组织 `com.openjiuwen.core` 的 API 页面，建议先浏览包 README，再下钻到子包页或类型页查看公开 API。

## 说明

- 文档结论以仓库中的 Java 源码和代表性测试为准。
- 导航按包层级展开，`SUMMARY.md`、共享入口页与包 README 使用同一棵目录树。
- 当前导航已覆盖 `application`、`common`、`context`、`controller`、`foundation`、`graph`、`memory`、`multiagent`、`operator`、`retrieval`、`runner`、`security`、`session`、`singleagent`、`sysop` 和 `workflow` 子树。

| 模块入口 | 说明 |
| --- | --- |
| [`com.openjiuwen.core`](./com.openjiuwen.core.README.md) | openJiuwen Java 核心命名空间入口，汇总 `application`、`common`、`context`、`controller`、`foundation`、`graph`、`memory`、`multiagent`、`operator`、`retrieval`、`runner`、`security`、`session`、`singleagent`、`sysop` 和 `workflow` 等模块。 |
