# com.openjiuwen.core.controller.legacy.config.ReflectorConfig

## class ReflectorConfig

```java
public class ReflectorConfig
```

`ReflectorConfig` 是旧版 Reflector 组件的占位配置类型。

## 说明

- 当前源码没有显式字段或方法，仅作为 `ReasonerConfig` 的组成部分保留。
- 文档保留该页面，方便兼容旧版配置结构。
