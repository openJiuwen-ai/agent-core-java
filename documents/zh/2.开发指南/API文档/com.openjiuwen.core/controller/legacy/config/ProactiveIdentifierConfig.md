# com.openjiuwen.core.controller.legacy.config.ProactiveIdentifierConfig

## class ProactiveIdentifierConfig

```java
public class ProactiveIdentifierConfig
```

`ProactiveIdentifierConfig` 是旧版主动识别模块的占位配置类型。

## 说明

- 当前源码没有显式字段或方法，仅作为 `ReasonerConfig` 组合配置的一部分存在。
- 文档保留该页面，便于兼容旧版配置层级。
