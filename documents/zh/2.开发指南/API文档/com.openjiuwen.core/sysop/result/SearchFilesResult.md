# com.openjiuwen.core.sysop.result.SearchFilesResult

## 类 SearchFilesResult

```java
public class SearchFilesResult extends BaseResult<SearchFilesData>
```

搜索文件操作的结果类型。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public SearchFilesResult(int code, String message, SearchFilesData data)` | 创建 `SearchFilesResult` 实例。 |

## Lombok 说明

- 该类型使用 `NoArgsConstructor`, `SuperBuilder` 生成访问器、builder 或构造辅助代码。
- 字段表基于源码中的显式字段整理；单独列出的方法仅包含显式声明的方法。
