# com.openjiuwen.core.sysop.result.UploadFileResult

## 类 UploadFileResult

```java
public class UploadFileResult extends BaseResult<UploadFileData>
```

上传文件操作的结果类型。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public UploadFileResult(int code, String message, UploadFileData data)` | 创建 `UploadFileResult` 实例。 |

## Lombok 说明

- 该类型使用 `NoArgsConstructor`, `SuperBuilder` 生成访问器、builder 或构造辅助代码。
- 字段表基于源码中的显式字段整理；单独列出的方法仅包含显式声明的方法。
