# com.openjiuwen.core.sys_operation.result.WriteFileResult

## 类 WriteFileResult

```java
public class WriteFileResult extends BaseResult<WriteFileData>
```

写入文件操作的结果类型。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public WriteFileResult(int code, String message, WriteFileData data)` | 创建 `WriteFileResult` 实例。 |

## Lombok 说明

- 该类型使用 `NoArgsConstructor`, `SuperBuilder` 生成访问器、builder 或构造辅助代码。
- 字段表基于源码中的显式字段整理；单独列出的方法仅包含显式声明的方法。
