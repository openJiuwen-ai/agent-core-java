# com.openjiuwen.core.sys_operation.result.ExecuteCodeStreamResult

## 类 ExecuteCodeStreamResult

```java
public class ExecuteCodeStreamResult extends BaseResult<ExecuteCodeChunkData>
```

代码执行操作的流式结果类型。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public ExecuteCodeStreamResult(int code, String message, ExecuteCodeChunkData data)` | 创建 `ExecuteCodeStreamResult` 实例。 |

## Lombok 说明

- 该类型使用 `NoArgsConstructor`, `SuperBuilder` 生成访问器、builder 或构造辅助代码。
- 字段表基于源码中的显式字段整理；单独列出的方法仅包含显式声明的方法。
