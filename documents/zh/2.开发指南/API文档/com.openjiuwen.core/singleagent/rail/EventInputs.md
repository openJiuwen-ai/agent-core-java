# com.openjiuwen.core.single_agent.rail.EventInputs

## 接口 EventInputs

```java
public interface EventInputs
```

所有事件输入载荷共享的标记接口。

## 说明

- 相关测试：`ReActAgentEvolveTest`、`ReActAgentTest`、`RailDataClassesTest`。
- `InvokeInputs`、`ModelCallInputs` 与 `ToolCallInputs` 都实现了该接口。
