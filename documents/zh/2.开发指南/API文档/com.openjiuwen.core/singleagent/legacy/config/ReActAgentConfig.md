# com.openjiuwen.core.singleagent.legacy.config.ReActAgentConfig

## 类 ReActAgentConfig

```java
public class ReActAgentConfig extends LegacyReActAgentConfig
```

`LegacyReActAgentConfig` 的兼容别名。

## 构造方法

| 签名 | 说明 |
|---|---|
| `public ReActAgentConfig()` | 调用父类默认构造后，通过 `LegacyApi.emitDeprecationWarning(...)` 输出弃用告警。 |

## 说明

- 类已标记 `@Deprecated(since = "0.1.7", forRemoval = true)`。
- 新代码应改用 `LegacyReActAgentConfig` 或现代 `com.openjiuwen.core.singleagent.agents.ReActAgentConfig`。
