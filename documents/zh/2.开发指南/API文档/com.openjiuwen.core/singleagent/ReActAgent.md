# com.openjiuwen.core.singleagent.ReActAgent

## 类 ReActAgent

```java
public class ReActAgent extends com.openjiuwen.core.singleagent.agents.ReActAgent
```

对 `com.openjiuwen.core.singleagent.agents.ReActAgent` 的顶层便捷别名。

## 构造方法

| 签名 | 说明 |
|---|---|
| `public ReActAgent(AgentCard card)` | 使用 `AgentCard` 创建实例，并转发给 `agents.ReActAgent`。 |

## 说明

- 相关测试：`AgentCallbackManagerTest`、`ReActAgentConfigTest`、`ReActAgentEvolveTest`、`ReActAgentTest`、`BaseAgentTest`。
