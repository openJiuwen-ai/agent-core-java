# db

`com.openjiuwen.core.foundation.store.db` 当前提供一个基于 JDBC 的默认数据库引擎持有器，供键值存储等组件复用。

## 类型

| 类型 | 说明 |
| --- | --- |
| [`DefaultDbStore`](db/DefaultDbStore.md) | 使用 `DataSource` 包装 JDBC 连接能力的默认实现。 |
