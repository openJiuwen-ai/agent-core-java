# com.openjiuwen.core.workflow.components.flow.SubWorkflowComponent

## 类 SubWorkflowComponent

```java
public class SubWorkflowComponent extends SubWorkflowComponentImpl
```

旧版兼容子工作流组件包装类型，复用 `SubWorkflowComponentImpl`。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public SubWorkflowComponent(Workflow subWorkflow)` | 创建 `SubWorkflowComponent` 实例。 |

## 说明

- 该类型位于旧版兼容包路径，用于兼容历史导入方式。
