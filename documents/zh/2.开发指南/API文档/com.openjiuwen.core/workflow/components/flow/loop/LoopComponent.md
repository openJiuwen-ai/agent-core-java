# com.openjiuwen.core.workflow.component.LoopComponent

## 类 LoopComponent

```java
public class LoopComponent extends LoopComponentImpl
```

旧版兼容循环组件类型，复用新版 `LoopComponentImpl` 的构造方式。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public LoopComponent(com.openjiuwen.core.workflow.component.loop.LoopGroup loopGroup, Map<String, Object> outputSchema)` | 创建 `LoopComponent` 实例。 |

## 说明

- 该类型位于旧版兼容包路径，用于兼容历史导入方式。
