# com.openjiuwen.core.graph.BranchComponent

## 类 BranchComponent

```java
public class BranchComponent extends com.openjiuwen.core.graph.BranchComponent
```

旧版兼容分支组件别名，复用当前分支实现。

## 说明

- 该类型位于旧版兼容包路径，用于兼容历史导入方式。
