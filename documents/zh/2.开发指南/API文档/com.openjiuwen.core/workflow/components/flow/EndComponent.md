# com.openjiuwen.core.workflow.components.flow.EndComponent

## 类 EndComponent

```java
public class EndComponent extends End
```

旧版兼容结束节点别名，表示工作流出口。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public EndComponent()` | 创建 `EndComponent` 实例。 |
| `public EndComponent(Map<String, Object> confMap)` | 创建 `EndComponent` 实例。 |
| `public EndComponent(EndConfig conf)` | 创建 `EndComponent` 实例。 |

## 说明

- 该类型位于旧版兼容包路径，用于兼容历史导入方式。
