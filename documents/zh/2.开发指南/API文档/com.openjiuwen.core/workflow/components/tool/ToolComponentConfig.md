# com.openjiuwen.core.workflow.component.tool.ToolComponentConfig

## 类 ToolComponentConfig

```java
public class ToolComponentConfig extends com.openjiuwen.core.workflow.component.tool.ToolComponentConfig
```

旧版兼容 Tool 配置包装类型，补充位置参数构造方法以兼容旧调用方式。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public ToolComponentConfig(String toolId)` | 创建 `ToolComponentConfig` 实例。 |
| `public ToolComponentConfig()` | 创建 `ToolComponentConfig` 实例。 |

## 说明

- 该类型位于旧版兼容包路径，用于兼容历史导入方式。
