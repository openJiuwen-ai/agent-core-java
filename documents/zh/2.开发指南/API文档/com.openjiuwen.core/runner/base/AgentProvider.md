# com.openjiuwen.core.runner.base.AgentProvider

## interface AgentProvider

```java
public interface AgentProvider<T> extends Supplier<T>
```

`AgentProvider` 定义 `com.openjiuwen.core.runner.base` 下的扩展契约。
