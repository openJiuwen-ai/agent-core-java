# com.openjiuwen.core.runner.base.WorkflowProvider

## interface WorkflowProvider

```java
public interface WorkflowProvider extends Supplier<Workflow>
```

`WorkflowProvider` 定义 `com.openjiuwen.core.runner.base` 下的扩展契约。
