# com.openjiuwen.core.runner.base.ModelProvider

## interface ModelProvider

```java
public interface ModelProvider extends Supplier<Model>
```

`ModelProvider` 定义 `com.openjiuwen.core.runner.base` 下的扩展契约。
