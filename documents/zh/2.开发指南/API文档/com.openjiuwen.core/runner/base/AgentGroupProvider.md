# com.openjiuwen.core.runner.base.AgentGroupProvider

## interface AgentGroupProvider

```java
public interface AgentGroupProvider<T> extends Supplier<T>
```

`AgentGroupProvider` 定义 `com.openjiuwen.core.runner.base` 下的扩展契约。
