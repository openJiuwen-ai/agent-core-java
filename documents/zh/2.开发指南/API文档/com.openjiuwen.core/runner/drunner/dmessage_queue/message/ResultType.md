# com.openjiuwen.core.runner.drunner.dmessage_queue.message.ResultType

## 枚举 ResultType

```java
public enum ResultType
```

ResultType 表示远端响应携带的是正常结果还是错误结果。

## 枚举值

| 枚举值 | 说明 |
| --- | --- |
| `MESSAGE` | 正常业务结果。 |
| `ERROR` | 远端执行失败后返回的错误结果。 |
