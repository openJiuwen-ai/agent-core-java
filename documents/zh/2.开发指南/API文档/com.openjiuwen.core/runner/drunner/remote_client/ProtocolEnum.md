# com.openjiuwen.core.runner.drunner.remote_client.ProtocolEnum

## 枚举 ProtocolEnum

```java
public enum ProtocolEnum
```

ProtocolEnum 定义远程调用使用的传输协议。

## 枚举值

| 枚举值 | 说明 |
| --- | --- |
| `MQ` | 基于消息队列的传输协议。 |
