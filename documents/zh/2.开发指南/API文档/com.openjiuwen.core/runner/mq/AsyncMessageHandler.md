# com.openjiuwen.core.runner.mq.AsyncMessageHandler

## 接口 AsyncMessageHandler

```java
public interface AsyncMessageHandler<T, R>
```

`AsyncMessageHandler` 定义 `com.openjiuwen.core.runner.mq` 下的扩展契约。
