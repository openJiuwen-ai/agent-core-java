# openjiuwen.agent_teams.monitor

Java output:

- `com.openjiuwen.agent_teams.monitor.MonitorPackage`

Mirrors Python's `openjiuwen/agent_teams/monitor/__init__.py`.

This package facade records the public monitor exports from Python `__all__`
without implementing the runtime monitor or stream logger in this task.
