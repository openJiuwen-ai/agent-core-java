# 团队技能演�?

团队技能演进是 agent-core 提供的多 Agent 协作技能自动化创建和在线演进功能，支持从执行轨迹中提炼可复用的团队协作模式�?

## 核心概念

### Team Skill

团队技能是一种特殊类型的 Skill，包含以下文件结构：

```
skills/<skill-name>/
├── SKILL.md          # 入口文件（YAML frontmatter + Markdown body�?
�?                     # frontmatter 必须包含 kind: team-skill
├── roles/
�?  ├── <role-id>.md  # 每个角色的详细定�?
�?  └── ...
├── workflow.md       # 工作流描述（任务依赖、并�?串行关系�?
├── bind.md           # 约束信息（轮数上限、超时、质量门�?
└── evolutions.json   # 演进记录
```

与普�?Skill 不同，Team Skill�?

- `kind: team-skill` 标识类型
- `roles` 列表定义角色及其 skills/tools 配置
- 适用于多 Agent 协作场景

### 演进机制

| 模块 | 功能 |
|------|------|
| `TeamSkillCreateRail` | 自动检测协作模式，建议创建团队技�?|
| `TeamSkillRail` | 在线演进 public rail：轨迹分析、用户请求、聚合式经验记录生成/审批。它�?`TeamSkillEvolutionRail` 的兼�?public alias�?|
| `TeamSkillExperienceOptimizer` | LLM 驱动的经验记录生成。`TeamSkillOptimizer` 保留为兼�?alias�?|
| `ExperienceScorer` | 经验评分和简化维�?|
| `InMemoryTrajectoryRegistry` | 运行�?source/sink，用于发布成�?snapshot 并聚合团队轨迹证�?|
| `TeamTrajectoryAggregator` | 面向存量轨迹的离�?调试聚合工具 |

---

## 自动创建

### 原理

`TeamSkillCreateRail` �?`AFTER_TASK_ITERATION` 回调中检�?`spawn_member` 调用次数。达到阈值时，注�?follow_up 提示，引�?Agent 通过 `ask_user` 确认并调�?`team-skill-creator` 技能�?

### 配置示例

```java
import com.com.openjiuwen.harness.rails import TeamSkillCreateRail
import com.openjiuwen.harness import create_deep_agent

create_rail = TeamSkillCreateRail(
    skills_dir="/path/to/skills",
    language="cn",
    min_team_members_for_create=2,  # spawn_member >= 2 时触�?
)

agent = create_deep_agent(
    model=model_client,
    system_prompt="你是一个团�?leader...",
    tools=team_tools,
    rails=[create_rail],
    enable_task_loop=True,
    workspace="/path/to/workspace",
)
```

### 创建流程

1. Agent 执行协作流程（调�?`build_team`、`spawn_member`、`create_task`�?
2. Rail 检�?`spawn_member` 调用次数达到阈�?
3. Rail 通过 `TaskLoopController.enqueue_follow_up()` 注入提示
4. Agent 调用 `ask_user` 工具向用户确�?
5. 用户选择"创建"后，Agent 调用 `team-skill-creator` 技�?
6. 技能根据轨迹生�?Team Skill 文件

---

## 在线演进

### TeamSkillRail 配置

```java
from openjiuwen.agent_evolving.trajectory import InMemoryTrajectoryRegistry
import com.openjiuwen.harness import create_deep_agent
import com.com.openjiuwen.harness.rails import TeamSkillRail

trajectory_registry = InMemoryTrajectoryRegistry()

team_rail = TeamSkillRail(
    skills_dir="/path/to/skills",
    llm=model_client,
    model="gpt-4",
    language="cn",
    team_id="research-team",
    trajectory_source=trajectory_registry,
    trajectory_sink=trajectory_registry,
    auto_save=False,           # 生成的记录需要用户审�?
    async_evolution=True,      # 异步执行演进
    evolution_total_timeout_secs=600.0,
)

agent = create_deep_agent(
    model=model_client,
    tools=team_tools,
    rails=[team_rail],
    skills=["research-team"],  # 加载已有团队技�?
)
```

`trajectory_source` �?`trajectory_sink` 是团队轨迹聚合的运行时接入点。Rail �?invoke 结束后把 `MemberTrajectorySnapshot` 发布�?sink；`TeamSkillRail` 在被动演进或用户主动演进需要团队级证据时，�?source 读取聚合轨迹�?

若要聚合多个成员，需要让所有贡献轨迹证据的 rail �?agent 都发布到同一�?`trajectory_sink`；`TeamSkillRail` 再通过 `trajectory_source` 读取这个共享 registry�?

`MemberTrajectorySnapshot` 只包含发布内容：`team_id`、`session_id`、`member_id`、`member_role`、`trajectory` �?`recorded_at_ms`。snapshot 新旧判定�?`InMemoryTrajectoryRegistry` 负责：`recorded_at_ms` 更新者优先；时间相同则使�?registry 的接收顺序，后接收者优先�?

### 触发时机

| 触发方式 | 触发条件 |
|----------|----------|
| 被动触发 | `view_task` 返回"所有任务已完成" |
| 主动触发 | 用户调用 `request_user_evolution()` |

### 演进路径

#### 1. 轨迹问题分析

Rail 分析团队执行轨迹，检测以下问题：

- **角色配合问题**: 协作断裂、数据未传�?
- **约束违反**: 超时、产出格式不合规
- **流程低效**: 重复调用、多余步�?
- **角色能力不足**: 某角色多次失败或产出质量不达�?

检测到问题时，Rail 会聚合信号并调用 `TeamSkillExperienceOptimizer.generate_records(EvolutionContext)` 生成经验记录�?

#### 2. 用户请求演进

用户主动提供改进建议�?

```java
result = await team_rail.request_user_evolution(
    skill_name="research-team",
    user_intent="增加 reviewer 角色，限�?research 时间不超�?10 分钟",
)
```

Rail 会先使用当前 rail 轨迹�?`trajectory_source` 聚合后的团队轨迹作为证据窗口检测轨迹问题，再将用户意图作为补充方向并入同一�?`generate_records(EvolutionContext)` 流程来生成经验记录�?

### 记录审批流程

1. Rail 生成经验记录并暂�?
2. 暂存记录通过 `OutputSchema` 发送到 TUI
3. 用户选择"Accept"�?Reject"
4. 审批时调�?`approve_record(request_id)` 写入 `evolutions.json`

```java
# 获取待审批事�?
events = await team_rail.drain_pending_approval_events(wait=True)

for event in events:
    if event.type == "chat.ask_user_question":
        request_id = event.payload["request_id"]
        # 用户审批
        await team_rail.approve_record(request_id)
```

---

## 经验管理

### 评分体系

ExperienceScorer 使用三维度加权评分：

| 维度 | 公式 | 权重 |
|------|------|------|
| E (Effectiveness) | Beta(1,1) 贝叶斯平�?| 0.5 |
| U (Utilization) | `times_used / times_presented` | 0.3 |
| F (Freshness) | 时间衰减 + 版本过期惩罚 | 0.2 |

**综合评分**: `score = 0.5 * E + 0.3 * U + 0.2 * F`

### 简化操�?

```java
result = await team_rail.request_simplify(
    skill_name="research-team",
    user_intent="清理过时经验",
)

# 返回: {"deleted": N, "merged": N, "refined": N, "kept": N, "errors": N}
```

| 操作 | 说明 |
|------|------|
| DELETE | 删除低质量经验（score < 阈值） |
| MERGE | 合并相似经验 |
| REFINE | 优化冗长内容 |
| KEEP | 保留高质量经�?|

### 重建流程

```java
prompt = await team_rail.request_rebuild(
    skill_name="research-team",
    user_intent="基于历史经验全面优化",
    min_score=0.5,  # 仅保�?score >= 0.5 的经�?
)

# prompt 为重建指令文本，需注入�?Agent 执行
```

重建流程�?

1. 归档当前 `SKILL.md` �?`evolutions.json` �?`archive/`
2. 筛选高质量演进经验（score >= min_score�?
3. 构建重建提示，调�?`teamskill-creator` 技能生成新版本
4. 清空 `evolutions.json`

---

## 最佳实�?

### 何时使用

- �?Agent 协作场景（`spawn_member >= 2`�?
- 需要沉淀协作经验、形成可复用模式
- 团队技能需要持续改进和优化

### 配置建议

| 配置�?| 建议 |
|--------|------|
| `min_team_members_for_create` | 根据团队规模调整，建�?2-3 |
| `auto_save` | 重要演进建议 `False`，确保审�?|
| `async_evolution` | 长任务建�?`True`，避免阻�?|
| `evolution_total_timeout_secs` | 根据任务复杂度调整，建议 300-900 |

### 定期维护

1. 每周执行 `request_simplify()` 清理低质量经�?
2. 每月评估是否需�?`request_rebuild()` 重建
3. 重要演进前设�?`auto_save=False` 确保用户审批

### 注意事项

- `TeamSkillCreateRail` �?`TeamSkillRail` 可同时使�?
- `TeamSkillCreateRail` 依赖 `enable_task_loop=True` �?DeepAgent
- 演进生成的内容需用户审批才能生效
