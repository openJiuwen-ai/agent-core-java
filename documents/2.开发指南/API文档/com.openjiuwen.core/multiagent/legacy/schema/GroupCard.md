# com.openjiuwen.core.multiagent.legacy.schema.GroupCard

## class GroupCard

```java
@Deprecated
public class GroupCard extends LegacyGroupCard
```

`GroupCard` 是 legacy 分组卡片的包级别别名，用于兼容历史导入路径。

## 说明

- 该类型不新增字段或方法，完整字段集合来自 `LegacyGroupCard`。
- `LegacyCompatibilityAliasTest` 通过该别名验证了 `setTopic(...)` / `getTopic()` 仍可正常使用。
