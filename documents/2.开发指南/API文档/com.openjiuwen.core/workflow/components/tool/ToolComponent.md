# com.openjiuwen.core.workflow.components.tool.ToolComponent

## 类 ToolComponent

```java
public class ToolComponent extends com.openjiuwen.core.workflow.component.tool.ToolComponent
```

旧版兼容 Tool 组件包装类型，复用新版 `component.tool.ToolComponent`。

## 构造方法

| 签名 | 说明 |
| --- | --- |
| `public ToolComponent(com.openjiuwen.core.workflow.component.tool.ToolComponentConfig config)` | 创建 `ToolComponent` 实例。 |
| `public ToolComponent(ToolComponentConfig config)` | 创建 `ToolComponent` 实例。 |

## 说明

- 该类型位于旧版兼容包路径，用于兼容历史导入方式。
