# Agent Teams Workspace Baseline

The Java team workspace baseline currently exposes:

- `TeamWorkspaceConfig`
- `WorkspaceMode`
- `ConflictStrategy`
- `WorkspaceFileLock`
- `TeamWorkspaceManager`

This first layer covers shared workspace metadata, mount helpers, and local lock handling.
