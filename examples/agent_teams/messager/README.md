# Agent Teams Messager Baseline

The Java baseline now exposes:

- `MessagerTransportConfig`
- `MessagerPeerConfig`
- `SubscriptionHandle`
- `EventMessage`
- `InProcessMessager`
- `MessagerFactory.createMessager`

Current backend support is `inprocess`.
