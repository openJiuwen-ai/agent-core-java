# RL NL2SQL Baseline

The current Java baseline aligns to the reusable training primitives behind the Python NL2SQL RL example:

- `Case`
- `CaseLoader`
- `ExactMatchMetric`
- `Trainer`

Helper entry:

- `examples.agent_evolving.AgentEvolvingExampleSupport#nl2sqlCaseLoader`
