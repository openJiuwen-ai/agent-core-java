# Hierarchical Teams Baseline

The Java baseline now exposes:

- `HierarchicalToolsTeamConfig`
- `HierarchicalToolsTeam`
- `HierarchicalMsgBusTeamConfig`
- `HierarchicalMsgBusTeam`

Suggested verification:

```bash
mvn -Dtest=HierarchicalTeamsCompatibilityTest test
```
