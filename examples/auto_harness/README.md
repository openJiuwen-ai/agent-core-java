# Auto Harness Baseline Example

The Java auto harness baseline currently exposes:

- `AutoHarnessConfig`
- `OptimizationTask`
- `AutoHarnessOrchestrator`
- `PipelineRegistry`
- `StageRegistry`

This first layer covers pipeline selection and task-cycle result aggregation. The deeper Python pipeline internals are still being ported incrementally.
