# RL Calculator Baseline

The current Java baseline aligns to the reusable training primitives behind the Python calculator RL example:

- `Case`
- `CaseLoader`
- `ExactMatchMetric`
- `Trainer`

Helper entry:

- `com.openjiuwen.agentevolving.AgentEvolvingExampleSupport#calculatorCaseLoader`
