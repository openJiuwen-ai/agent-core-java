# OpenAPI MCP Example

Java baseline:

- builds an `openapi` `McpServerConfig`
- intended for direct client / tool / workflow integration examples

Reference support:

- `com.openjiuwen.core.foundation.tool.mcp.McpExampleSupport#openApiConfig`
